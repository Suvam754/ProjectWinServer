Pull both the below images:
docker pull suvam754/projectwinserver-web-app
docker pull suvam754/projectwinserver-sql-server

Be on the folder where docker-compose.yml file is present.

Run below command
docker-compose up